import React, { memo } from 'react';
import './footer.scss';

export default memo(function Footer() {
    return (
        <div className="footer" id="footer">
            footer.
        </div>
    )
})
